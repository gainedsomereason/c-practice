#include "acllib.h"

int Set

/*

ACLLib是一个基于Win32API的函数库，
实际提供一个.c和两个.h，可以在MSVC和Dev C++(MinGW)中使用，
以GPL方式开源在github上

Windows API从第一个32位的Windows开始出现了，叫做Win32API
是一个纯c函数库，可以写Windows应用程序

main()成为C语言的入口函数与C语言本身无关，
你的代码被一小段叫做启动程序的代码所调动，它需要一个叫main的地方
操作系统把可执行程序装载到内存里，启动运行，然后调用main函数

Win32API程序调用的启动程序是WinMain()

*/
